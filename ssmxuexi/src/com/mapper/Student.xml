<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.mapper.StudentMapper">
		<resultMap type="Student" id="studentResult">
			<id property="id" column="id"/>
			<result property="username" column="username"/>
			<result property="name" column="name"/>
			<result property="sex" column="sex"/>
			<result property="banji" column="banji"/>
			<result property="pwd" column="pwd"/>
		</resultMap>
		<insert id="insert" parameterType="Student">
			insert into student
			<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="username != null">username,</if>
			<if test="name != null">name,</if>
			<if test="sex != null">sex,</if>
			<if test="banji != null">banji,</if>
			<if test="pwd != null">pwd,</if>
			</trim>
			<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="username != null">#{username},</if>
			<if test="name != null">#{name},</if>
			<if test="sex != null">#{sex},</if>
			<if test="banji != null">#{banji},</if>
			<if test="pwd != null">#{pwd},</if>
			</trim>
		</insert>
		<update id="update" parameterType="Student">
			update student
			<trim prefix="set" suffixOverrides=",">
			<if test="username != null">username=#{username},</if>
			<if test="name != null">name=#{name},</if>
			<if test="sex != null">sex=#{sex},</if>
			<if test="banji != null">banji=#{banji},</if>
			<if test="pwd != null">pwd=#{pwd},</if>
		</trim>
			where id=#{id}
		</update>
		<delete id="delete" parameterType="Student">
			delete from student where id= #{id}
		</delete>
		<select id="list" resultType="Student" resultMap="studentResult">
			select * from  student where 1=1 ${where} order by id desc
			</select>
		<select id="sealist" resultType="Student" resultMap="studentResult">
			select * from  student where 1=1 ${where} 
			</select>
		<select id="get" parameterType="_int" resultType="Student">
			select * from  Student  where id= #{id}
		</select>
			<select id="total" resultType="int">
	        select count(*) from student    
	    </select>
<update id="updatesql"  parameterType="String">
${value}
</update>
<delete id="deletesql"  parameterType="String">
${value}  
</delete>
	</mapper>